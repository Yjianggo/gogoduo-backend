# 2026-04-26 工作记录

## 1. 云开发环境续费提醒

云环境 cloud1-5gr0rd3r8c63a492（个人版）将于 2026-05-11 到期，¥19.9/月，建议续费开连续订阅。

## 2. 开发网页版在线考试系统

将小程序功能完整移植为纯静态 HTML 网页版，路径：`web/index.html`

**功能清单：**
- 答题页：姓名+手机号登录，支持单/多/判/填/问答五种题型
- 题型标签切换、导航跳题、倒计时自动交卷
- 成绩页：分数、正确率、及格状态展示
- 管理后台（密码 admin123）：考试设置、题目管理、成绩查看
- 批量导入（【分隔】格式）、导出 JSON/CSV
- 数据全部存 localStorage，零依赖零费用

**部署方式：**
- 最简单：把 index.html 直接发给同事用浏览器打开
- 推荐：上传到 Vercel（免费）获得公网链接
- 局限：localStorage 各自独立，成绩不能集中汇总（如需要可升级后端）

## 3. 升级 Supabase 云端版（完成）

应喻江要求，将网页版从 localStorage 升级为 Supabase 云数据库，实现全员成绩汇总。

**改造内容：**
- `web/index.html` 重写数据层：SB 对象封装 Supabase REST API（select/upsert/insert/update/delete）
- 所有题目、成绩、设置读写均走 Supabase REST 接口
- 首次使用新增「配置页」：填写 URL + Anon Key 后连接
- 成绩提交增加降级逻辑：Supabase 失败时先存本地 pendingRecords
- 管理后台「成绩查看」可看到所有人的成绩，实时云端刷新
- `web/README.md` 完整重写，包含 Supabase 注册、创建项目、初始化 SQL 三步教程

**使用流程：**
1. 喻江注册 supabase.com，创建免费项目
2. SQL Editor 运行初始化 SQL（创建三张表，关闭 RLS）
3. 复制 Project URL + Anon Key，填入网页配置页
4. 部署到 Vercel 或直接发 index.html 文件
5. 所有人通过同一 Supabase 数据库，管理员可实时查看全员成绩

**Supabase 免费套餐：** 500MB 存储 + 5万行数据，100人考试/1000道题完全够用。
