# 2026-04-20 工作记录

## 今日修复内容

### 1. 修复 app.js 云初始化缺失问题
- 问题：app.js 没有 wx.cloud.init()，导致所有云函数调用报 "Cloud API isn't enabled"
- 修复：在 app.js 顶部添加 wx.cloud.init({ env: 'cloud1-5gr0rd3r8c63a492', traceUser: true })

### 2. 修复 exam_records 数据库权限问题
- 问题：scores.js 读取 exam_records 报 "database permission denied"
- 修复：在云开发控制台将 exam_records 集合权限设置为 {"read": true, "write": true}

### 3. 成绩页正常显示（3人成绩可见）
- 修复后成绩页成功从云端读取数据，显示考试人数3、平均分27、及格率33%

### 4. 实现题目云端同步功能
- 新建云函数：getExamQuestions（读取 exam_questions 集合）
- 新建云函数：saveExamQuestions（保存题目到 exam_questions 集合，_id=main_questions）
- 修改 admin/questions.js：每次保存/删除/批量导入题目后调用 syncQuestionsToCloud()
- 修改 exam.js：onLoad 时先调用 loadQuestionsFromCloud()，再调用 loadExamSettings()
- 需要在云开发控制台创建 exam_questions 集合，权限 {"read": true, "write": true}
- 需要上传 getExamQuestions 和 saveExamQuestions 云函数

### 5. 修复及格分数未同步问题（今日）
- 问题：result.js 从本地 examSettings 读取 passScore，其他用户本地没有设置，默认60分
- 修复：result.js 改为先显示本地值，再调用 getExamSettings 云函数获取最新及格分
- 修复后：用户答完题后，结果页会从云端读取管理员设置的及格分（如90分）

### 6. 题目管理页批量删除功能（今日）
- 新增：右上角"批量删除"按钮，点击进入批量模式
- 批量模式：每道题目左侧出现复选框，顶部工具栏显示全选和"删除选中"按钮
- 删除选中按钮无选中时灰色不可点，有选中时变红并可点
- 删除后自动同步云端，同时退出批量模式

### 7. 题目管理页调整顺序功能（今日）
- 右上角新增"调整顺序"按钮，点击进入排序模式（与批量删除互斥）
- 排序模式：每道题显示左侧蓝色序号，右侧出现 ⇈↑↓⇊ 四个按钮（上移、下移、置顶、置底）
- 顶部黄色提示条提醒用户操作方式
- 点击右上角"完成排序"自动保存并同步到云端
- 底部添加/导入按钮在排序模式下自动隐藏

### 8. 修复云端数据同步到本地缓存问题（今日）
- 问题：管理员保存考试标题/题目到云端后，其他用户读不到最新数据
- 根因：exam.js 和 admin/questions.js 读取云端成功后没有同步到本地存储，下次降级读取时用旧数据
- 修复 exam.js：loadQuestionsFromCloud 成功后 wx.setStorageSync('adminQuestions', questions)
- 修复 admin/exam.js：saveExamSettings 成功后 wx.setStorageSync('examSettings', settings)
- 修复 exam.js：loadExamSettings 成功后 wx.setStorageSync('examSettings', settings)
- 修复 admin/questions.js：loadQuestions 改为优先从云端读取，成功后同步到本地

### 9. 题目管理页添加手动保存按钮（今日）
- 底部新增「💾 保存题目」按钮（橙色），点击后本地+云端同步保存
- 三按钮排列：📥 批量导入 | + 添加题目 | 💾 保存题目
- 同时添加 manualSave() 方法，同步保存到 wx.setStorageSync + syncQuestionsToCloud

## 当前版本状态
- 最新版本：1.2.6（待上传，含批量删除、调整顺序、数据同步修复、手动保存按钮）
- 上传后需操作：管理员重新进入题目管理页（自动从云同步），管理员重新保存一次设置

## 项目信息（备查）
- AppID: wx9339cce26f0b3ed7
- 云环境ID: cloud1-5gr0rd3r8c63a492
- 项目路径: c:\Users\Y\WorkBuddy\20260412150507
- miniprogramRoot: miniprogram/
- cloudfunctionRoot: cloudfunctions/
- 正确导入方式：选择完整项目目录 20260412150507（不要只选 miniprogram 子目录）
